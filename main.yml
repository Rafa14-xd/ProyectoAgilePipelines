# Nombre del pipeline
name: CI AzurePipeline

# Configuración de triggers
trigger:
  branches:
    include:
      - main  # Activa el pipeline en la rama main

pr:
  branches:
    include:
      - main  # Activa el pipeline en pull requests a la rama main

# Definición de etapas y trabajos
jobs:
- job: Build
  pool:
    vmImage: 'ubuntu-latest'
  steps:
    - task: Checkout@1  # Comando para hacer checkout del código
    - task: NodeTool@0
      inputs:
        versionSpec: '14.x'  # Especifica la versión de Node.js
    - script: npm install
      displayName: 'Install dependencies'
    - script: npm test
      displayName: 'Run Tests'

- job: Lint
  pool:
    vmImage: 'ubuntu-latest'
  steps:
    - task: Checkout@1
    - script: npm run lint
      displayName: 'Run Linter'

- job: IntegrationTests
  dependsOn:  # Especifica dependencias de otros trabajos
    - Build
    - Lint
  pool:
    vmImage: 'ubuntu-latest'
  steps:
    - task: Checkout@1
    - script: npm run test:integration
      displayName: 'Run Integration Tests'
